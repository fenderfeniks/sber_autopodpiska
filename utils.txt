# uv
uv venv
uv sync --no-dev (сборка докер образа)(core+api)
uv sync (для разработки)(core+api+dev)
uv sync --extra eda (для экспериментов и визуализации) (все что выше)

#.vscode settings
for .vscode settings.json
{
    "python.defaultInterpreterPath": "${workspaceFolder}/.venv/Scripts/python.exe",
    "python.analysis.extraPaths": [
        "${workspaceFolder}/src",
        "${workspaceFolder}"
    ],
    "python.analysis.typeCheckingMode": "off",
    "python.analysis.useLibraryCodeForTypes": true
}
